use (
	"lists"
	"functions"
)

export let (
	only functions.bind(functions.only, isError)
	pass functions.bind(functions.only, functions.not(isError))
)

export fn any(l) {
	let first lists.first(isError, l)
	return len(first) == 0 ? l : first[0]
}
