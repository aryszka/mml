use (
	"lists"
	"functions"
)

export let (
	only functions.bind(functions.only, isError)
	pass functions.bind(functions.only, functions.not(isError))
)

export fn any(l) lists.fold(fn (c, r) isError(r) ? r : isError(c) ? c : [r..., c], [], l)
